<Window x:Class="SignalRChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat Client" Height="350" Width="400">
    
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Name"/>
        <TextBox Grid.Row="1" 
                 x:Name="UserTextBox" 
                 Margin="0,0,0,5"/>

        <TextBlock Grid.Row="2" 
                   Text="Message"
                   Margin="0,10,0,0"/>
        <TextBox Grid.Row="3"    
                 x:Name="MessageTextBox" Margin="0,0,0,5"/>
        
        <Button Grid.Row="4" 
                Content="Send"
                Height="28"
                Width="75"
                HorizontalAlignment="Left"
                Click="SendButton_Click" 
                Margin="0,0,0,10"/>
        
        <ListBox Grid.Row="5" 
                 x:Name="MessagesList"/>
        
    </Grid>
    
</Window>
